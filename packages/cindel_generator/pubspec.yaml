name: cindel_generator
description: Code generator for Cindel schemas, serializers, and query helpers.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  analyzer: ">=9.0.0 <10.0.0"
  build: ^4.0.6
  source_gen: ^4.2.3
  cindel_annotations:
    path: ../cindel_annotations

dev_dependencies:
  build_runner: ^2.15.0
  lints: ^6.1.0
  test: ^1.31.1
